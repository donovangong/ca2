name: 2_Functional_Test

on:
  workflow_dispatch:
  workflow_call:

jobs:
  functional_test:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: test/package-lock.json

      - name: Install coverage tools
        run: npm --prefix test ci

      - name: Run functional tests
        run: |
          npx --prefix test c8 --reporter=lcov --reporter=text --report-dir coverage/functional --include "services/frontend/**/*.js" node test/functional/run-functional-tests.js

      - name: Upload functional coverage
        uses: actions/upload-artifact@v7
        with:
          name: functional-coverage
          path: coverage/functional/lcov.info
