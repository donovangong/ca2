name: 5_Pro_Env_Deploy

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: Image tag to deploy
        required: false
        default: latest
  workflow_call:
    inputs:
      image_tag:
        required: false
        type: string
        default: latest

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Prepare kubeconfig
        run: |
          cp /etc/rancher/k3s/k3s.yaml $RUNNER_TEMP/k3s.yaml
          chmod 600 $RUNNER_TEMP/k3s.yaml

      - name: Backup database
        run: |
          mkdir -p resources/backup
          if KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n ca2 get deployment/postgres >/dev/null 2>&1; then
            POSTGRES_POD=$(KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n ca2 get pod -l app=postgres -o jsonpath='{.items[0].metadata.name}')
            KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n ca2 exec "$POSTGRES_POD" -- pg_dump --clean --if-exists -U postgres -d orders_db > resources/backup/latest-backup.sql
            echo "BACKUP_CREATED=true" >> "$GITHUB_ENV"
          else
            echo "No existing ca2 PostgreSQL deployment found. Skipping database backup."
          fi

      - name: Upload database backup
        if: env.BACKUP_CREATED == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: database-backup
          path: resources/backup/latest-backup.sql

      - name: Clean Kubernetes resources
        run: |
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl delete namespace ca2 || true
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl wait --for=delete namespace/ca2 --timeout=60s || true

      - name: Install KEDA
        run: |
          if ! KUBECONFIG=$RUNNER_TEMP/k3s.yaml helm status keda -n keda >/dev/null 2>&1; then
            KUBECONFIG=$RUNNER_TEMP/k3s.yaml helm repo add kedacore https://kedacore.github.io/charts --force-update
            KUBECONFIG=$RUNNER_TEMP/k3s.yaml helm repo update
            KUBECONFIG=$RUNNER_TEMP/k3s.yaml helm upgrade --install keda kedacore/keda -n keda --create-namespace
          fi
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n keda rollout status deployment/keda-operator --timeout=120s
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n keda rollout status deployment/keda-operator-metrics-apiserver --timeout=120s
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n keda rollout status deployment/keda-admission-webhooks --timeout=120s

      - name: Deploy with Helm
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
        run: |
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml helm upgrade --install ca2 ./resources/helm/ca2 -n ca2 --create-namespace --set-string images.frontend=ca2-frontend:$IMAGE_TAG --set-string images.productService=ca2-product-service:$IMAGE_TAG --set-string images.orderService=ca2-order-service:$IMAGE_TAG

      - name: Wait for application rollout
        run: |
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n ca2 rollout status deployment/postgres --timeout=180s
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n ca2 rollout status deployment/product-service --timeout=180s
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n ca2 rollout status deployment/order-service --timeout=180s
          KUBECONFIG=$RUNNER_TEMP/k3s.yaml kubectl -n ca2 rollout status deployment/frontend --timeout=180s
