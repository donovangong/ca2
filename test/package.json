{
  "name": "ca2-order-inventory-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:services": "npm --prefix ../services/product-service ci && npm --prefix ../services/order-service ci",
    "test:api:deployed": "node api/run-api-tests.js"
  },
  "devDependencies": {
    "c8": "^10.1.3"
  }
}
